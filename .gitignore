# Local data and secrets
data/
.env
.env.*
!.env.example

# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
dist/
build/
.eggs/

# Virtual environments
.venv/
venv/
env/

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db
desktop.ini

# Testing
.pytest_cache/
.coverage
htmlcov/

# SQLite temp files
*.db-journal
*.db-wal
*.db-shm

# Never commit SQLite databases anywhere in the repo
*.db

# Machine-specific values (real VM identity etc. — placeholders live in RUNBOOK)
docs/local-notes.md

# --- Private, local-only docs (the soul) â€” never committed in the public repo ---
# The real copies now live in ../../brain/ (a separate private repo).
# These rules stay as a tripwire in case a file ever reappears here.
config/personal_brand.md
docs/brand/
docs/GLOSSARY.md
docs/ROADMAP.md
docs/plans/
docs/local-notes.md

# Reference/source images belong in the private brain repo, not in a public one.
# docs/assets/ holds only images the README and docs actually embed.
docs/assets/WhatsApp*
docs/assets/IMG_*
docs/assets/Screenshot*
